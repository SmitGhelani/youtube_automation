# ── LLM: Gemini (replaces Claude — free tier) ──────────────────────────────

# ── Video: fal.ai Seedance (paid per-use, free credits on signup) ───────────
# No extra SDK needed — uses requests directly (REST API)
requests>=2.31.0

# ── TTS: Kokoro (primary — local, free, near-human quality) ─────────────────
kokoro-onnx>=0.4.0
soundfile>=0.12.1
numpy>=1.24.0
scipy>=1.11.0

# ── Image: Pillow (fallback scene generation) ────────────────────────────────
Pillow>=10.0.0

# ── YouTube upload ────────────────────────────────────────────────────────────
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# ── Trend / SEO helpers ───────────────────────────────────────────────────────
pytrends>=4.9.2

# ── Notifications ─────────────────────────────────────────────────────────────
sendgrid>=6.10.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
anthropic>=0.25.0
# google-genai>=1.0.0  # optional: new Gemini SDK (replaces google-generativeai)
